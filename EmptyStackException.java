/**
 * `EmptyStackException` is a custom exception class that extends `RuntimeException`.
 * It is used to indicate that an operation on a stack was attempted when the stack was empty.
 */
public class EmptyStackException extends RuntimeException {
    /**
     * Initializes a new instance of the `EmptyStackException` class.
     */
    public EmptyStackException() {
    }
}
